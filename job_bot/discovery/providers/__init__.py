# Disabled: Greenhouse scrapes US/European tech company roles.
# User wants African-focused opportunities instead.
# from job_bot.discovery.providers.greenhouse import GreenhouseProvider
# from job_bot.discovery.providers.lever import LeverProvider
# from job_bot.discovery.providers.ashby import AshbyProvider

ATS_PROVIDERS = [
    # GreenhouseProvider(),  — US/European tech companies, not Africa-focused
    # LeverProvider  — API deprecated (404)
    # AshbyProvider  — API now requires auth (403)
]
