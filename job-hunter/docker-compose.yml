services:
  app:
    build: .
    container_name: job-hunter
    restart: unless-stopped
    ports:
      - "8001:8000"
    volumes:
      - ./data:/app/data
    environment:
      - TZ=Africa/Kigali
